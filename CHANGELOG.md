# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)；`版本号+构建号` 对应 `pubspec.yaml` 的 `version`。

## 未发布

### 新增

- 内容语言设置：可按 10% 档位指定英文内容占比；哲学、词条、经典、小说与教程会按设置优先选择相应语言。
- 扩充中文公版经典、白话作品与英文书目，并增加预置书目体检与诗词选集生成工具。

### 修复

- 设置变化不再重建信息流仓库；改字号、主题或内容源后返回阅读页，已加载卡片和当前位置保持不丢失。
- 统一六个内容源的抓取接口，修复诗词源在内容语言策略下的调用兼容性。

### 变更

- 词条与哲学内容共用维基候选抓取组件，避免两类卡片选题重叠；扩展古登堡章节与维基文库正文的清洗、去目录和去重处理。

- 添加 MIT 许可证（`LICENSE`），仓库由私有转为公开。
- `pubspec.yaml` 补充 `homepage` / `repository` / `issue_tracker` 元信息。

## 0.1.1 - 2026-09-11

### 修复
- **预取在 Provider 销毁后仍访问 `ref`**：`FeedController.loadMore` 会抛
  `UnmountedRefException`（退出/切后台时出现）。改为在 `build()` 中固定仓库引用，
  并在每个 `await` 之后判断 `ref.mounted`；`FeedPage` 的首帧回调同样加了保护。
  该问题由 Android 真机验收用例发现。

### 变更
- **「实用技能」源质量**：wikiHow 不可达时退到中文维基教科书，之前会抽到教科书式
  理论章节（如「皮肤性病学/细菌性皮肤病」）且会重复。新增「实用主题根名白名单」
  （食谱 / 急救 / 手工 / 园艺 / 摄影 / 收纳 / 健身…）与会话内去重（最近 12 条不重复），
  抽样由 `3/10 为学术页 + 2 次重复` 改进为 `12/12 均为可跟做内容、无重复`。

### 交付与验证
- Android release APK：通用包 + arm64-v8a + armeabi-v7a。
- iOS 未签名 IPA（Sideloadly 签名安装），由 GitHub Actions 在 macOS 上构建。
- 49 个单元/Widget 测试通过；`flutter analyze` 无告警。
- Android 设备端验收用例通过；连续上滑 50 张压测 **47s（平均 0.95s/张，最慢 1.23s）**，
  六个内容源均在出卡。原始日志见 `docs/evidence/`。

## 0.1.0 - 2026-09-11

### 新增
- 六个内容源的纯文字信息流：冷门学术词条（Wikidata + 维基百科）、哲学观点（SEP）、
  经典选段（古登堡 / 维基文库）、短篇小说与散文、实用技能（wikiHow / 维基教科书）、
  诗词名句（今日诗词）。
- 卡片态 / 展开态两种布局：卡片态单屏 + 向底部渐隐；点击展开后可滚动阅读，
  滑到底继续上滑即切下一张；再点一次收起。
- 兔子洞：词条内链点击后在当前位置插入新卡，深度上限 5 层。
- 设置：逐源开关、字号三档、深浅主题、词条冷门度（≤100 / ≤20）、清除缓存。
- 可靠性：并发竞速出卡、失败换源重抽、单源连续失败熔断 60s、无网回落本地缓存。

下载：[v0.1.1 测试版](https://github.com/x192038743-sudo/ApexScrolling/releases/tag/v0.1.1)
