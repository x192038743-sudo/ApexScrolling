# ApexScrolling 测试版（v0.1.0）说明

## 1. 这是一版什么版本

- 版本号：`0.1.0+1`（测试版 / beta），Flutter 3.35 双端工程，本次交付 **Android release APK**。
- 定位：功能完整、可日常试用的第一个测试版；内容与交互按《项目规划》v1 全量实现。
- 签名：使用 Flutter 模板的 **debug keystore** 签名（便于直接安装试用），**上架前必须换成正式签名**。

## 2. 安装

APK 产物（仓库内已放好一份，见 `dist/`；`build/` 下的原始产物同名）：

| 文件 | 体积 | 适用 |
|---|---|---|
| `dist/ApexScrolling-v0.1.0-beta-universal.apk` | 45.7MB | 任意机型（含 arm64 / armv7 / x86_64） |
| `dist/ApexScrolling-v0.1.0-beta-arm64-v8a.apk` | 16.3MB | 绝大多数现代手机 |
| `dist/ApexScrolling-v0.1.0-beta-armeabi-v7a.apk` | 13.9MB | 老 32 位机型 |

校验值见 `dist/SHA256SUMS.txt`。

```bash
# 用 adb 安装（手机开启「USB 调试」）
adb install -r build/app/outputs/flutter-apk/app-release.apk
```

也可以直接把 APK 传到手机点击安装（需在系统设置里允许「安装未知来源应用」）。

最低系统版本沿用 Flutter 默认（Android 5.0 / API 21+），目标 API 36。

## 3. 首次打开会看到什么

1. 冷启动后先取第一张卡。若首个抽中的是「冷门词条」，需要等一次 Wikidata 查询（约 10–15 秒），期间会显示加载页；其余源通常 1–3 秒。之后候选池复用，出卡明显变快。
2. 卡片态：顶部粗体标题 + 小字来源署名，正文向下渐隐；**上滑下一张、下滑上一张**。
3. **轻点卡片**进入展开态，正文可滚动；**滑到正文底部继续上滑**直接切下一张；**再轻点一次**收起。
4. 词条卡展开后底部会出现「延伸词条」链接，点击即在当前位置插入下一层卡片（兔子洞，最多 5 层）。
5. 右上角 `≡` 打开设置：逐源开关、字号三档、主题、词条冷门度（≤100 / ≤20）、清除缓存。

## 4. 建议重点验证的项

| 项 | 期望表现 |
|---|---|
| 连续上滑 50 张 | 不白屏、不卡顿；每张都能正常出卡 |
| 任一源不可用 | 信息流不中断，自动换源；顶部出现轻提示 |
| 断网刷 | 显示本地缓存卡片，并提示「网络不可用 · 正在浏览缓存卡片」 |
| 展开长文（>10 屏） | 滚动流畅，滑到底继续上滑可切下一张 |
| 字号三档 / 深浅主题 | 立即生效，重启后保持 |
| 逐源开关 | 关掉的源不再出现；至少保留一个源 |
| 冷启动首卡 | 非词条源 1–3 秒内出卡 |

### 4.1 一条命令跑完的自动化验收（推荐）

计划书里的「集成验收」已固化成端到端测试，装好设备后直接跑：

```bash
flutter test integration_test/beta_acceptance_test.dart -d <device-id>
flutter test integration_test/feed_soak_test.dart -d <device-id>    # 连续上滑 50 张
```

它会用**真机真实网络**依次验证并打印证据：冷启动出卡（含卡片标题 / 字数 / 署名）、
卡片态无内滚且带渐隐、上滑切下一张、点击展开、展开态内滚动不误切卡、再点收起、
进入设置页并显示测试版版本号。首卡最长等待 150 秒（冷启动抽到 Wikidata 词条源时较慢）。

**已在 Android 模拟器（Android 11 / API 30，WHPX 硬件加速）上跑通**，原始日志见
`docs/evidence/`：

| 用例 | 结果 | 关键证据 |
|---|---|---|
| 真机验收 `beta_acceptance_test` | ✅ 全过（14s） | 首卡 `诗词名句/过华清宫绝句三首/16 字/今日诗词` → 上滑切换 `念奴娇·赤壁怀古` → 抽到长文卡 `The History of Utilitarianism（2189 字）` → 展开态内滚动 `offset 0→140` 未误切卡 → 滑到底继续上滑切到第 4 页 → 设置页显示 `v0.1.0+1 · 测试版` |
| 压测 `feed_soak_test`（连续上滑 50 张） | ✅ 全过（48s） | `50 张卡片，总耗时 47s，平均每张 0.95s，最慢 1.23s`；来源分布 `诗词名句=10，短篇小说=8，哲学观点=5，经典选段=12，实用技能=8，冷门词条=8`（六源全在出卡，无白屏无崩溃） |
| release APK 实测 | ✅ | 安装 `app-release.apk` → 启动 → 连续上滑 6 次：进程存活、`logcat` 无 `FATAL`/`E/flutter`、截图确认卡片已渲染（浅色主题 87.6% 亮底 + 341 行文字） |

### 4.2 跑真机测试时踩到的两个坑（已修）

1. **模拟器用软件 GPU 会卡死**：Flutter 3.35 在 Android 默认用 Impeller 渲染器，
   而 `emulator -gpu swiftshader_indirect` 没有可用的 Vulkan，App 一启动整个 guest 就挂死。
   改用 `-gpu host`（本机走 WHPX + ranchu Vulkan）后 48 秒开机、稳定运行。
   复现命令：
   ```bash
   emulator -avd <name> -no-window -no-audio -no-boot-anim -no-snapshot \
            -gpu host -memory 4096 -cores 4
   ```
2. **预取在 Provider 销毁后仍用 `ref`**（真机测试抓到的真实缺陷）：
   `FeedController.loadMore` 抛 `UnmountedRefException`，会导致退出/切后台时异常。
   已改为在 `build()` 中固定仓库引用，并在每个 `await` 之后判断 `ref.mounted`。

### 4.3 「实用技能」源质量修正

wikiHow 在受限网络不可达时会退到中文维基教科书，之前会抽到教科书式的理论章节
（如「皮肤性病学/细菌性皮肤病」「社会医学/英国国家卫生服务制度」）且有重复。
现在加了「实用主题根名白名单」（食谱 / 急救 / 手工 / 园艺 / 摄影 / 收纳 / 健身…）
与会话内去重（最近 12 条不重复），抽样对比：

```
修正前 10 次：3 次学术/特刊类、2 次重复
修正后 12 次：12/12 全部为菜谱 / 摄影 / 园艺等可跟做内容，无重复
```

## 5. 已知问题与限制

1. **wikiHow 在部分网络不可达**（本机开发环境即如此），此时「实用技能」卡改由中文维基教科书提供，形态是「简介 + 编号步骤」，署名与授权同步切换为 CC BY-SA 4.0。
2. **维基百科主域 `zh.wikipedia.org` 在部分网络不可达**，因此正文统一走 `api.wikimedia.org`；若该域也被拦截，词条源会熔断并由其他源接管。
3. 词条卡的正文是条目**导语**（1–4 段），内容量小于小说/散文卡，属设计如此。
4. 古登堡书籍全文按需下载（数百 KB–1MB），首次抽到某本书时略慢，之后走内存缓存。
5. 测试版使用 **debug 签名**（上架前必须替换正式签名）。应用图标与启动底色已定制：
   图标为深色底 + 衬线 A + 琥珀下划线（源图 `assets/branding/app_icon_1024.png`，
   由 `python tool/make_icon.py` 一次性生成 Android mipmap 与 iOS AppIcon），
   启动底色与主题一致（浅色 #FAF7F1 / 深色 #0F0F11），不再出现白屏闪烁。

## 6. 在别的机器上重新构建

```bash
flutter config --android-sdk <你的 Android SDK 路径>
flutter config --jdk-dir <JDK 17 路径>
flutter build apk --release
```

构建相关的两点说明：

- `android/gradle.properties` 里的 `apex.cnMirrors=true` 会让 Gradle 优先走国内镜像（华为云 / 腾讯云 / 阿里云）解析 AGP、AndroidX、Kotlin 插件；如果你的网络能直连 `maven.google.com`，把它改成 `false` 即可走官方仓库。
- 若你的网络需要 Flutter 官方资源镜像，构建前设置：`set FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn`。首次构建还会自动下载 Gradle 发行包与 Android SDK 组件（含 NDK，若工程中保留 `ndkVersion`）。

## 7. 验证记录（本次交付）

- `flutter analyze`：无告警。
- `flutter test`：49 个测试全部通过（适配器解析、段落切分、熔断/换源/离线兜底、卡片手势与展开收起、设置页交互与持久化）。
- `dart run tool/smoke.dart`：真实网络冒烟 6/6 数据源可用（词条 / 哲学 / 经典 / 小说 / 技能 / 诗词各出一张真实卡片）。
- `flutter build apk --release`：产出 `app-release.apk`，v0.1.0+1，minSdk 24 / targetSdk 36，debug 签名；`aapt2 dump badging` 校验包名、版本、INTERNET 权限与标签正常。
- `flutter build bundle`：编译校验通过。

另外做过的产物级校验（本机没有可用的真机/模拟器，故用静态方式确认运行时装配）：

- `aapt2 dump badging`：包名 `com.apexscrolling.apex_scrolling`、版本 `0.1.0`、`minSdk 24 / targetSdk 36`、
  `INTERNET` 权限、应用名 `ApexScrolling`、图标五个密度齐全。
- `aapt2 dump files` / 解包核对：三种 ABI 的 `libflutter.so` + `libapp.so`、`classes.dex`、`flutter_assets` 均在包内。
- `apksigner verify`：签名有效（Android Debug 证书）。
- `apkanalyzer` + R8 mapping：`MainActivity`、`GeneratedPluginRegistrant` 与
  `io.flutter.plugins.sharedpreferences.*` 都在 dex 中（release 默认开启 R8，符号表在
  `build/app/outputs/mapping/release/mapping.txt`，可用于还原线上堆栈）。
